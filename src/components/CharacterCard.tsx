import "./CharacterCard.css";

const CharacterCard = () => {
  return (
    <div className="CharacterCard">
      CharacterCard works and will hold Name, Race, Level(?), and a picture if
      provided.
    </div>
  );
};

export default CharacterCard;
